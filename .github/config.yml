# 📄 Image Description Plugin Configuration

id: plugin-image-description
title: "Vault Image Description Plugin"
type: plugin
category: obsidian-plugin
section: plugins
role: plugin
folder: system/plugins
tags: [obsidian, plugin, images, description, alttext]
version: 1.0

# 📊 Status & Lifecycle
status: stable
visibility: public
created: 2025-05-30
updated: 2025-05-30

# 📚 Context & Description
summary: >
  Generate alt text for images directly inside Obsidian using a modular,
  VaultOS-friendly plugin.

description: |
  The plugin analyses images in your vault and writes descriptive alt text so
  your notes remain accessible. It uses TypeScript and Rollup under the hood and
  follows the VaultOS folder conventions for easy maintenance.

purpose: "Improve accessibility by describing images in your notes."
scope: "Images stored inside an Obsidian vault"
use_cases: ["automatic alt text", "accessibility", "image cataloging"]
audience: ["note takers", "accessibility advocates", "plugin developers"]

# 🧱 Relationships
parent: "obsidian-plugins"
children: []
friends: []
related: []

# 🌐 Dynamic GitHub URLs
github:
  base: "https://github.com/<%= github.user %>/<%= github.repo %>"
  manifest: "<%= github.base %>/blob/main/manifest.json"
  readme: "<%= github.base %>/blob/main/README.md"
  contributing: "<%= github.base %>/blob/main/CONTRIBUTING.md"
  license: "<%= github.base %>/blob/main/LICENSE"
  changelog: "<%= github.base %>/blob/main/change-log.md"
  discussions: "<%= github.base %>/discussions"
  sponsor: "https://github.com/sponsors/<%= github.user %>"

# ⚙️ Obsidian Plugin Specific
plugin:
  manifest_path: "manifest.json"
  entry_file: "src/main.ts"
  output_dir: "dist/"
  build_tool: "rollup"
  script_language: "ts"

# 💌 Attribution
author: "Charlie (PtiCalin)"
maintainers: ["<%= github.user %>"]
sponsor_link: "<%= github.sponsor %>"
